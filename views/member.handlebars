{{>header}}
<div class="row">
        <div class="col col-12 text-center">
                <h4>Member Page<h4>
                                {{>followGroupForm}}
                                {{>viewMember}}
                                {{>plannerEvent}}
                                