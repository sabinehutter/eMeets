{{!-- Need Header --}}
{{>header}}

<div class="container-fluid">
    <div>
        {{!-- 6 groups handlebars --}}
        {{>joinGroup}}
        {{>upcomingEventsView}}
        {{>membersViewSection}}
        {{>createEventView}}
        {{!-- post comment --}}
    </div>
</div>

{{!-- Need Footer --}}
{{>footer}}