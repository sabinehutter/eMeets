<div class="col-md-12 text-center enter_section">
  {{log groups}}
  {{#each groups}}
  <div>{{this.dataValues.group_name}}</div>
  {{/each}}
  <form action="/api/follower" method="POST">
    <input type="text" class="form-control" name="comment" value="" placeholder="add your comment" id="enter_text">
    <button type="submit" class="btn btn-dark" id="text-enter-button">Follow</button>
  </form>
</div>