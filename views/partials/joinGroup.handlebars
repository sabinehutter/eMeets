<div class="row">
    <div class="col col-12 text-center">
         {{log groups}}
        {{#each groups}}
        <div>{{this.dataValues.group_name}}</div>   
        {{/each}}     
        <a href="#"><button>Join Group</button></a>
    </div>
</div>
</div>