{{!-- Need Header --}}

<div class="col-md-12 "
     id="top-bar">
   <h1> Sequelize box </h1>
</div>

<div class="container">
   <div class="row">


      {{!-- view cooment  --}}
      {{> viewComments}} 
      {{!-- 6 groups handlebars --}}

      {{!-- post comment --}}
      {{> postComments}}
      {{!--  Join Group & Create Group handlebars--}}

   </div>
</div>

{{!-- Need Footer --}}